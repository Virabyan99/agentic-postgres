# Capacity envelope

**Generated by `bin/render-capacity-envelope.py --write`. Do not edit by hand.**

What this deployment does at its limits, and — just as much — what has not
been measured and why. `CAP-ENV-001`.

Every number below carries the conditions it was sampled under, because a
latency without its concurrency is a number about nothing (D593, D603). And
every number says whether it **transfers**: a limit that follows from
`pool_size` holds wherever the deployment runs, while a figure in
milliseconds describes the machine that produced it and nothing else.

---

## The images these numbers describe

The envelope is pinned to these three. If one moves, the measurements below
are claims about a previous version and `--check` says so, naming the image.
It is pinned to three rather than to the whole lock deliberately: an envelope
that went stale when an unrelated image moved would cry wolf, and
`traefik:v3.7` moved twice inside Session 14 alone (D787).

- `POSTGRES_IMAGE` = `docker.io/pgvector/pgvector:pg18@sha256:691673308c99d2161ba298736f3147f1f22d79de2fb7ec93ae9b4afcab870b62`
- `PGBOUNCER_IMAGE` = `docker.io/edoburu/pgbouncer:v1.24.1-p1@sha256:3db3d7223e93af52b4116f642951a1a5fa44702a88c2a59cf7562cac19320c9e`
- `POSTGREST_IMAGE` = `docker.io/postgrest/postgrest:v14.16@sha256:bea1c76a856fa39d1e542d25911cf95d02fe2bf971992d033044ff209f1504b8`

---

## What was measured

### Pooled clients: the pooler queues rather than refusing

**80 concurrent clients against 20 server slots all completed, none refused**

Sampled under:

- pgbouncer in transaction mode, default_pool_size 20, max_client_conn 100
- each client one transaction holding a server slot for 200 ms
- query_wait_timeout 20 s, never reached at this holding time

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

Four times the pool with no error at all. The pooler's response to excess concurrency is latency, not refusal -- which is why the refusal threshold below is expressed in holding TIME rather than in client count.

### Pooled clients: latency against concurrency

**1 client 207 ms; 20 clients 240 ms; 40 clients 324 ms; 80 clients 476 ms (p50)**

Sampled under:

- 200 ms transactions, pgbouncer default_pool_size 20
- an 8 GB development machine, NOT the 3,814 MB deployment host
- single project, no other load on the cluster

**Does not transfer.** It describes the machine the rig ran on. Quoting it for the deployment host would be describing one machine with another's number.

The shape transfers and the milliseconds do not. Latency tracks queue depth times service time: at 80 clients against 20 slots, four waves of a 200 ms transaction is 800 ms and the observed maximum was 733 ms. **A deployment's own numbers must be taken on the deployment.**

### Pooled clients: what a caller sees when the queue times out

**ProtocolViolation: query_wait_timeout**

Sampled under:

- 30 clients each holding a slot for 25 s against query_wait_timeout 20 s
- exactly 20 completed and 10 were refused, matching default_pool_size

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

**The error class names the wrong cause.** A capacity condition is reported as a PROTOCOL violation, so a client catching `OperationalError` -- the usual 'connection trouble, retry' -- does not catch it. This is D145's family: the state is real and the signal describes something else. Compare the REST path below, which gets the same failure right.

### Pooled clients: the client-connection ceiling

**the 101st connection is refused with FATAL: no more connections allowed (max_client_conn)**

Sampled under:

- max_client_conn 100; 105 connections opened in sequence
- exactly 100 were accepted before the refusal

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

This one names its cause correctly, and it arrives as an `OperationalError`. **So the same component reports one limit honestly and the other as a protocol violation**, which is worth knowing before writing a retry.

### REST callers: the served plateau

**about 110 concurrent 500 ms requests are served; the rest are refused**

Sampled under:

- PGRST_DB_POOL 10, PGRST_DB_POOL_ACQUISITION_TIMEOUT 5 s
- each request holds its connection for 500 ms
- measured at 100, 120, 160 and 240 concurrent

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

The plateau is stable: 110 served at 160 concurrent and 110 at 240. Offered load above the plateau does not reduce what is served, which is the property that makes this a limit rather than a collapse. **PostgREST connects directly to the cluster, not through the pooler**, so this limit and the pooled one are independent.

### REST callers: what a caller sees when the pool is exhausted

**HTTP 504 with {"code":"PGRST003","message":"Timed out acquiring connection from connection pool."}**

Sampled under:

- PGRST_DB_POOL 10, acquisition timeout 5 s, 500 ms requests
- first observed at 120 concurrent; 14 of 120 refused

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

**This is the honest one.** A machine-readable code, a message naming the actual cause, and a status a caller already classifies as a gateway timeout. Set beside the pooler's `ProtocolViolation`, it is the same failure reported two ways, and only one of them can be acted on without knowing this document.

### REST callers: the limit is connection-seconds, not requests

**240 concurrent fast requests were all served; none was refused**

Sampled under:

- the same 240 concurrency that refused 130 of the 500 ms requests
- a request doing no work beyond a constant select

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

The separating control, and it decides how the limit should be read. Neither the HTTP layer nor the caller count is the constraint -- what saturates is callers HOLDING a connection. **Capacity here is connection-seconds**, so halving a query's duration is worth as much as doubling the pool. (Those requests returned in single-digit milliseconds, but that figure is the rig machine's and is not quoted as this deployment's -- the transferring claim is that none of them was refused.)

### REST callers: the service is undamaged by saturation

**a request after 240-concurrent saturation returned 200 in 2 ms**

Sampled under:

- immediately after the 240-concurrent arm returned 130 refusals

**Transfers.** It follows from a setting rather than from a machine, so it holds wherever this deployment runs.

Checked because a limit that leaves wreckage is a different property from a limit that sheds load. This one sheds.

---

## What was not measured, and why

Listed rather than omitted. An envelope missing the scenarios nobody could
run reads as an envelope of the whole system — which is the dishonest
reporting this claim is most exposed to, arriving as a document that looks
complete rather than as a claim that is false.

### The deployment's own numbers, on the deployment

Every measurement here was taken off-host, against the pinned images at the rendered settings. The host is a 3,814 MB machine with no swap and eighteen containers; this rig was an 8 GB development machine running two. **The CONFIGURATION numbers transfer and the MACHINE numbers do not**, and no arithmetic converts one into the other.

*Unblocked by: the Run 8 host trip, which is the first deploy in three sessions.*

### MCP reads and writes

A write is four upstream requests and a read is three (ADR 0129), and **nothing has ever timed any of it against the deployment** — a standing open item since Session 8. Timing it needs the whole agent plane: the auth service, a signed token, the capability contract and a live audit table. That is a deployment, not a rig.

*Unblocked by: the Run 8 host trip.*

### Backup behaviour under load

It needs the pgBackRest repository, which is an R2 bucket reached with a credential this machine does not hold and must not be given. `process-max` is 1, so a 31 MB backup is six minutes of serialised round trips (D593) — the interaction worth measuring is what that does to query latency, and it cannot be simulated without the real repository.

*Unblocked by: the Run 8 host trip, on a project carrying no data anybody needs.*

### Timeout and pool tuning

The plan asks for tuning after the load scenarios. **Nothing is tuned here, deliberately**: every number above was measured off-host, and changing `pool_size` or `query_wait_timeout` on the strength of a development machine's latency would be tuning the deployment to a measurement that is not about it. `ALERT_ERROR_RATIO` (Run 5) is waiting on the same evidence.

*Unblocked by: the Run 8 host trip, and a second envelope taken there.*
