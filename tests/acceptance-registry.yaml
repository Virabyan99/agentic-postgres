# Acceptance registry — the single hand-authored source of requirement identity.
#
# This file is the ONLY place a requirement ID is created. Two documents are
# generated from it and drift-checked in CI:
#
#   docs/acceptance-matrix.md                       (whole file)
#   docs/product-contract.md § requirement catalog  (marker block)
#
# Hand-maintaining those alongside this file would drift, and the failure mode
# is silent: a P0 requirement listed in prose that no test covers. Generating
# them makes that structurally impossible rather than merely detectable.
#
# Every entry needs:
#   id              stable, unique, prefixed per runbook §4.2
#   priority        P0 | P1 | P2
#   target_session  1-12; the session that must make it pass
#   test_nodeids    exact Pytest node IDs, verified by real collection
#   description     what the requirement guarantees, not how it is tested
#
# A `future` marker's session and requirement must match the entry here.
# tests/contract/test_future_marker_policy.py enforces that in both directions.
#
# A node ID naming a parametrized test refers to all of its cases.

# ---------------------------------------------------------------------------
# Session 1 — active
# ---------------------------------------------------------------------------

- id: CFG-001
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_project_manifest.py::test_example_manifest_is_valid
    - tests/contract/test_project_manifest.py::test_example_manifest_carries_no_secret_material
  description: >-
    A project manifest validates against schema and semantics, and contains no
    secret material.

- id: CFG-002
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_yaml_parser.py::test_duplicate_key_is_rejected
    - tests/contract/test_yaml_parser.py::test_multiple_documents_are_rejected
    - tests/contract/test_yaml_parser.py::test_merge_keys_are_rejected
    - tests/contract/test_yaml_parser.py::test_non_string_keys_are_rejected
  description: >-
    Ambiguous YAML is rejected outright rather than resolved silently.
    Default PyYAML keeps the last value for a duplicate key.

- id: CFG-003
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_naming.py::test_every_role_stays_within_63_bytes
    - tests/contract/test_naming.py::test_roles_are_derived_independently_not_from_a_shared_prefix
    - tests/contract/test_naming.py::test_truncation_golden_vector
    - tests/contract/test_naming.py::test_long_shared_prefixes_do_not_collide
  description: >-
    Every identity is derived deterministically and per-context, and no
    PostgreSQL role can exceed 63 bytes regardless of input length.

- id: CFG-004
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_output_schema.py::test_repeated_render_is_byte_identical
    - tests/contract/test_output_schema.py::test_render_is_byte_identical_across_processes
    - tests/contract/test_output_schema.py::test_no_timestamp_reaches_rendered_output
    - tests/contract/test_naming.py::test_derivation_is_independent_of_pythonhashseed
  description: >-
    Identical inputs render byte-identical output, in the same process and
    across processes, with no timestamp anywhere in the document.

- id: CFG-005
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_output_schema.py::test_rendered_output_validates
    - tests/contract/test_output_schema.py::test_endpoints_are_unavailable_not_faked
    - tests/contract/test_output_schema.py::test_unavailable_endpoint_may_not_carry_a_url
    - tests/contract/test_output_schema.py::test_input_digests_are_real_and_correct
  description: >-
    Generated output conforms to its schema, records real input digests, and
    represents nonexistent endpoints as unavailable rather than as placeholders.

- id: CFG-006
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_output_schema.py::test_generated_files_are_owner_only
    - tests/contract/test_render_atomicity.py::test_write_private_sets_owner_only_mode_regardless_of_umask
  description: Every generated file is mode 0600, independent of the process umask.

- id: CFG-007
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_render_atomicity.py::test_failed_render_preserves_the_previous_output
    - tests/contract/test_render_atomicity.py::test_publish_failure_rolls_the_previous_directory_back
    - tests/contract/test_render_atomicity.py::test_failed_render_leaves_no_staging_residue
  description: >-
    A render that fails validation or publication leaves the previous valid
    render byte-identical and removes its staging directory.

- id: CFG-008
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_render_atomicity.py::test_symlinked_target_directory_is_refused
    - tests/contract/test_render_atomicity.py::test_symlinked_generated_root_is_refused
    - tests/contract/test_render_atomicity.py::test_symlinked_manifest_is_refused
  description: The renderer refuses symlinked inputs and output targets.

- id: CFG-009
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_project_manifest.py::test_sensitive_keys_are_rejected
    - tests/contract/test_project_manifest.py::test_safe_keys_are_not_false_positives
    - tests/contract/test_output_schema.py::test_rendered_output_carries_no_secret
  description: >-
    Secret-bearing keys are rejected in manifests and in output, without false
    positives for safe reference names such as password_secret_ref.

- id: CFG-010
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_project_manifest.py::test_public_pool_requires_a_cidr_allowlist
    - tests/contract/test_project_manifest.py::test_public_pool_rejects_a_default_route
  description: >-
    Public pooler exposure requires a specific CIDR allowlist; a default route
    is not an allowlist.

- id: CFG-011
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_project_manifest.py::test_reserved_route_collision_is_rejected
    - tests/contract/test_project_manifest.py::test_api_and_mcp_trees_may_not_overlap
    - tests/contract/test_project_manifest.py::test_similar_but_distinct_prefixes_are_allowed
  description: >-
    Route trees may not collide with a reserved route or with each other, and
    overlap is decided segment-wise rather than by string prefix.

- id: CFG-012
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_render_isolation.py::test_collision_count_is_zero
    - tests/contract/test_render_isolation.py::test_role_name_sets_are_fully_disjoint
    - tests/contract/test_render_isolation.py::test_project_scoped_identity_differs
  description: >-
    Two similar projects render fully disjoint identities, compared over parsed
    semantic fields rather than by duplicate-string search.

- id: CFG-013
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_capabilities_manifest.py::test_example_manifest_is_valid_and_empty
    - tests/contract/test_capabilities_manifest.py::test_enabled_capability_fails_without_a_live_contract
    - tests/contract/test_capabilities_manifest.py::test_sql_and_raw_query_fields_are_rejected
  description: >-
    The capability surface is empty by default, cannot be enabled without a
    live backing contract, and cannot express SQL or a raw query.

- id: CFG-014
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_version_lock.py::test_every_image_is_pinned_to_a_digest
    - tests/contract/test_version_lock.py::test_no_floating_tag_remains
    - tests/contract/test_version_lock.py::test_check_detects_an_edited_candidate_file
    - tests/contract/test_repository_contract.py::test_dependency_lock_uses_hashes
  description: >-
    Container images are pinned to immutable digests for one declared platform,
    Python dependencies are hash-locked, and drift is detected offline.

- id: CFG-015
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_compose_contract.py::test_rendered_resource_names_match_outputs
    - tests/contract/test_compose_contract.py::test_inherited_project_name_cannot_override_the_generated_one
    - tests/contract/test_compose_contract.py::test_inherited_image_cannot_override_the_locked_digest
    - tests/contract/test_compose_contract.py::test_container_starting_subcommands_are_refused
  description: >-
    The Compose model renders the exact resource names published in
    outputs.json, cannot be overridden by inherited environment variables, and
    refuses to start a container in Session 1.

- id: DX-002
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_cli_contract.py::test_help_exits_zero_and_says_something
    - tests/contract/test_cli_contract.py::test_future_stub_exits_ten
    - tests/contract/test_cli_contract.py::test_command_works_from_another_directory
    - tests/contract/test_cli_contract.py::test_commands_do_not_echo_a_planted_environment_variable
  description: >-
    Operator commands document themselves, obey the exit-code convention, work
    from any directory, and never print the environment.

- id: DX-003
  priority: P0
  target_session: 1
  test_nodeids:
    - tests/contract/test_repository_contract.py::test_required_path_exists
    - tests/contract/test_repository_contract.py::test_generated_output_is_ignored
    - tests/contract/test_repository_contract.py::test_deployable_source_does_not_hardcode_a_fixture_identity
    - tests/contract/test_repository_contract.py::test_source_specification_checksum_matches
  description: >-
    The repository has its required shape, generated output stays out of Git,
    and no deployable source file hard-codes a fixture identity.

# ---------------------------------------------------------------------------
# Session 2 — host, ingress, secrets
# ---------------------------------------------------------------------------

- id: SEC-NET-001
  priority: P0
  target_session: 2
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_public_routes_cannot_reach_direct_postgresql
  description: No public route reaches the direct PostgreSQL endpoint.

- id: SEC-SECRET-001
  priority: P0
  target_session: 2
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_secret_values_do_not_appear_in_images_logs_or_compose_output
  description: >-
    Secret values appear in no image, repository file, Compose output, or log.

# ---------------------------------------------------------------------------
# Session 3 — database authorization
# ---------------------------------------------------------------------------

- id: SEC-RLS-001
  priority: P0
  target_session: 3
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_user_a_cannot_access_user_b_rows
  description: A user can neither read nor mutate another user's rows.

- id: SEC-VIEW-001
  priority: P0
  target_session: 3
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_security_invoker_view_preserves_rls
  description: A security-invoker view preserves the underlying row policy.

- id: SEC-FUNC-001
  priority: P0
  target_session: 3
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_api_role_cannot_execute_ungranted_functions
  description: An API role cannot execute a function it was not explicitly granted.

- id: SEC-DEFAULT-001
  priority: P0
  target_session: 3
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_newly_created_function_is_not_executable_by_public
  description: Default EXECUTE on newly created functions is revoked from PUBLIC.

- id: SEC-OWNER-001
  priority: P0
  target_session: 3
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_object_owner_is_a_non_login_role
  description: Objects are owned by a non-login role that no service connects as.

# ---------------------------------------------------------------------------
# Session 4 — endpoints and clients
# ---------------------------------------------------------------------------

- id: DBX-001
  priority: P0
  target_session: 4
  test_nodeids:
    - tests/integration/test_future_database_clients.py::test_prisma_migrate_uses_the_direct_url
  description: Prisma Migrate runs through the direct endpoint.

- id: DBX-002
  priority: P0
  target_session: 4
  test_nodeids:
    - tests/integration/test_future_database_clients.py::test_prisma_client_uses_the_pooled_url
  description: Prisma Client operates through the pooled endpoint.

- id: DBX-003
  priority: P0
  target_session: 4
  test_nodeids:
    - tests/integration/test_future_database_clients.py::test_psql_works_on_both_endpoints
  description: psql connects through both the direct and pooled endpoints.

- id: DBX-004
  priority: P1
  target_session: 4
  test_nodeids:
    - tests/integration/test_future_database_clients.py::test_node_and_python_clients_work_through_the_pooler
  description: Node and Python drivers round-trip a query through the pooler.

- id: DBX-005
  priority: P0
  target_session: 4
  test_nodeids:
    - tests/integration/test_future_database_clients.py::test_direct_postgresql_is_not_publicly_reachable
  description: The direct endpoint is not publicly reachable.

# ---------------------------------------------------------------------------
# Session 5 — REST contract
# ---------------------------------------------------------------------------

- id: API-SCHEMA-001
  priority: P0
  target_session: 5
  test_nodeids:
    - tests/integration/test_future_api.py::test_only_the_api_schema_is_exposed
  description: Only the api schema is exposed, matching a committed allowlist.

- id: API-CACHE-001
  priority: P0
  target_session: 5
  test_nodeids:
    - tests/integration/test_future_api.py::test_api_migration_reloads_the_schema_cache_and_updates_openapi
  description: An API migration reloads the schema cache and updates OpenAPI.

- id: API-LIMIT-001
  priority: P0
  target_session: 5
  test_nodeids:
    - tests/integration/test_future_api.py::test_row_limits_and_timeouts_are_enforced_server_side
  description: Row limits and timeouts are enforced by the server, not the client.

- id: SEC-ANON-001
  priority: P0
  target_session: 5
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_anon_cannot_reach_protected_resources
  description: The anonymous role cannot reach protected resources.

- id: SEC-PRIV-001
  priority: P0
  target_session: 5
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_api_roles_cannot_reach_the_private_schema
  description: No API role can address the app or app_private schemas.

# ---------------------------------------------------------------------------
# Session 6 — identity
# ---------------------------------------------------------------------------

- id: SEC-JWT-001
  priority: P0
  target_session: 6
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_invalid_issuer_audience_algorithm_or_token_type_is_rejected
  description: >-
    Wrong issuer, audience, algorithm, token type, or expiry is rejected.

- id: SEC-KEY-001
  priority: P0
  target_session: 6
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_verifying_services_do_not_hold_the_private_signing_key
  description: Verifying services hold public material only.

- id: SEC-CRED-001
  priority: P0
  target_session: 6
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_raw_credentials_are_never_stored_or_logged
  description: Raw user and agent credentials are never stored or logged.

- id: API-AUTH-001
  priority: P0
  target_session: 6
  test_nodeids:
    - tests/integration/test_future_api.py::test_login_and_identity_endpoints_behave
  description: Login issues a short-lived token and the identity endpoint reflects it.

- id: API-ADMIN-001
  priority: P0
  target_session: 6
  test_nodeids:
    - tests/integration/test_future_api.py::test_admin_endpoints_require_explicit_admin_scope
  description: Admin endpoints require an explicit admin scope, not a role name.

# ---------------------------------------------------------------------------
# Session 7 — object storage
# ---------------------------------------------------------------------------

- id: STO-OWN-001
  priority: P0
  target_session: 7
  test_nodeids:
    - tests/integration/test_future_storage.py::test_cross_user_object_download_is_denied
  description: A user cannot obtain a download URL for another user's object.

- id: STO-KEY-001
  priority: P0
  target_session: 7
  test_nodeids:
    - tests/integration/test_future_storage.py::test_client_supplied_object_keys_are_rejected
  description: Object keys are generated server-side; client keys are rejected.

- id: STO-URL-001
  priority: P0
  target_session: 7
  test_nodeids:
    - tests/integration/test_future_storage.py::test_presigned_urls_never_reach_logs_or_the_audit_table
  description: A presigned URL never reaches a log or the audit table.

- id: STO-COMPLETE-001
  priority: P1
  target_session: 7
  test_nodeids:
    - tests/integration/test_future_storage.py::test_abandoned_upload_intents_are_not_downloadable
  description: Only objects verified against storage become downloadable.

# ---------------------------------------------------------------------------
# Session 8 — agent reads
# ---------------------------------------------------------------------------

- id: AGT-READ-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_mcp_read_equals_the_postgrest_result
  description: An agent read through MCP equals the equivalent PostgREST result.

- id: AGT-SQL-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_no_tool_accepts_sql_or_a_raw_query_string
  description: No agent input accepts SQL, a SQL fragment, or a raw query string.

- id: AGT-SCOPE-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_tool_discovery_respects_scopes
  description: Tool discovery is filtered by the caller's scopes.

- id: AGT-DRIFT-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_new_api_operation_does_not_become_agent_visible
  description: >-
    Adding an API operation does not expose an agent capability without an
    explicit capabilities.yaml change.

- id: AGT-BUDGET-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_response_row_and_byte_budgets_are_enforced
  description: Row and response-size budgets are enforced server-side.

- id: SEC-INJ-001
  priority: P0
  target_session: 8
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_injection_strings_remain_data
  description: An injection payload stays data and does not alter query structure.

# ---------------------------------------------------------------------------
# Session 9 — agent writes, audit, revocation
# ---------------------------------------------------------------------------

- id: SEC-REV-001
  priority: P0
  target_session: 9
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_revoked_token_is_denied_by_mcp_and_postgrest
  description: >-
    A token issued before revocation is denied on its next read and write
    through both MCP and PostgREST.

- id: SEC-PARAM-001
  priority: P0
  target_session: 9
  test_nodeids:
    - tests/security/test_future_security_boundaries.py::test_tool_parameters_cannot_override_identity_or_scope
  description: Tool parameters cannot override agent identity, role, or scope.

- id: AGT-WRITE-001
  priority: P0
  target_session: 9
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_read_only_agent_cannot_discover_or_invoke_writes
  description: A read-only agent can neither discover nor invoke a write.

- id: AGT-AUDIT-001
  priority: P0
  target_session: 9
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_all_tool_outcomes_are_audited_with_redaction
  description: >-
    Read, write, denied, and failed attempts are audited with redaction.

- id: AGT-AUDITFAIL-001
  priority: P0
  target_session: 9
  test_nodeids:
    - tests/integration/test_future_mcp.py::test_write_fails_closed_when_the_audit_record_cannot_be_created
  description: A write fails closed when its audit record cannot be created.

# ---------------------------------------------------------------------------
# Session 10 — recovery
# ---------------------------------------------------------------------------

- id: REC-PITR-001
  priority: P0
  target_session: 10
  test_nodeids:
    - tests/recovery/test_future_pitr.py::test_timestamp_targeted_restore_succeeds
  description: A timestamp-targeted restore into a disposable volume succeeds.

- id: REC-SAFE-001
  priority: P0
  target_session: 10
  test_nodeids:
    - tests/recovery/test_future_pitr.py::test_restore_never_touches_the_active_volume
  description: >-
    The restore path never mounts, overwrites, or mutates the active volume.

- id: REC-SMOKE-001
  priority: P0
  target_session: 10
  test_nodeids:
    - tests/recovery/test_future_pitr.py::test_restored_instance_passes_schema_and_rls_smoke_checks
  description: The restored instance passes schema, RLS read, and write-RPC checks.

- id: REC-EVID-001
  priority: P0
  target_session: 10
  test_nodeids:
    - tests/recovery/test_future_pitr.py::test_restore_evidence_records_the_required_fields
  description: >-
    Restore evidence records backup set, requested and achieved recovery point,
    RTO, schema version, and test outcomes.

- id: REC-WAL-001
  priority: P1
  target_session: 10
  test_nodeids:
    - tests/recovery/test_future_pitr.py::test_wal_archiving_failure_is_visible
  description: A WAL archiving failure produces a visible non-zero signal.

# ---------------------------------------------------------------------------
# Sessions 11 and 12 — deployment, operations, reuse
# ---------------------------------------------------------------------------

- id: DEP-001
  priority: P0
  target_session: 11
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_fresh_project_deploys_on_an_empty_host
  description: A fresh project deploys on an empty host from documentation alone.

- id: DEP-002
  priority: P0
  target_session: 11
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_redeploy_is_idempotent_and_preserves_data
  description: Re-running deployment converges without destroying data.

- id: DEP-PRE-001
  priority: P0
  target_session: 11
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_failed_prerequisite_stops_before_changing_the_deployment
  description: >-
    A missing prerequisite stops deployment before it changes anything, and
    lists every absent item.

- id: OPS-001
  priority: P0
  target_session: 11
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_doctor_reports_required_checks_without_secrets
  description: The diagnostic command reports every required check without secrets.

- id: OPS-LOG-001
  priority: P1
  target_session: 11
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_request_id_propagates_across_the_stack
  description: One request ID propagates across ingress, API, agent, and audit records.

- id: DEP-ISO-001
  priority: P0
  target_session: 12
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_two_projects_share_no_state_or_authority
  description: >-
    Two projects on one host share no state or authority; shared provider
    accounts are permitted, shared project scope is not.

- id: DEP-REMOVE-001
  priority: P0
  target_session: 12
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_removing_the_second_project_does_not_affect_the_first
  description: Removing one project does not affect another.

- id: DX-001
  priority: P0
  target_session: 12
  test_nodeids:
    - tests/contract/test_future_deployment.py::test_new_team_member_completes_the_documented_path
  description: >-
    A developer who did not build the primitive completes the documented path
    without source edits or undocumented commands.
